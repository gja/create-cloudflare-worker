{
  "name": "create-cloudflare-worker",
  "version": "1.0.1",
  "description": "Create a Cloudflare worker",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gja/create-cloudflare-worker.git"
  },
  "keywords": [
    "cloudflare",
    "worker"
  ],
  "author": "Tejas Dinkar <tejas@gja.in>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gja/create-cloudflare-worker/issues"
  },
  "bin": "./bin/create-cloudflare-worker",
  "homepage": "https://github.com/gja/create-cloudflare-worker#readme",
  "devDependencies": {
    "standard-version": "^4.4.0"
  },
  "scripts": {
    "prepublishOnly": "npm install && git diff --quiet && npx standard-version && git push --follow-tags origin"
  }
}
